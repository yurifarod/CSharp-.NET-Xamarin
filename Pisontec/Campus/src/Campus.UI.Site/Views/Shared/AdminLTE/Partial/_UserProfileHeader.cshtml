
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager

@if (SignInManager.IsSignedIn(User))
{
    //var u = UserManager;
    //var id = UserManager.GetUserId();
    //var id = System.Web.HttpContext.Current.User.Identity.GetUserId();

    //var u = HttpContext.Current.GetOwinContext()
     //       .GetUserManager<ApplicationUserManager>().FindByIdAsync(id).Result;

    <li class="dropdown user user-menu">
        <!-- Menu Toggle Button -->
        <a href="#" class="dropdown-toggle" data-toggle="dropdown">
            <!-- The user image in the navbar-->
            <img src="~/_AdminLTE/dist/img/user.svg" class="user-image" alt="User Image">
            <!-- hidden-xs hides the username on small devices so only the image appears. -->
            <span class="hidden-xs">@UserManager.GetUserName(User)</span>
        </a>
        <ul class="dropdown-menu">
            <!-- The user image in the menu -->
            <li class="user-header">
                <img src="~/_AdminLTE/dist/img/user.svg" class="img-circle" alt="User Image">

                <p>
                    @if (UserManager != null)
                    {
                        @(UserManager.GetUserName(User))
                    }
                    @*<small>Member since Nov. 2012</small>*@
                </p>
            </li>
            <!-- Menu Body -->
            @*<li class="user-body">
                    <div class="row">
                        <div class="col-xs-4 text-center">
                            <a href="#">Followers</a>
                        </div>
                        <div class="col-xs-4 text-center">
                            <a href="#">Sales</a>
                        </div>
                        <div class="col-xs-4 text-center">
                            <a href="#">Friends</a>
                        </div>
                    </div>

                </li>*@
            <!-- Menu Footer-->
            <li class="user-footer">
                <div class="pull-left">
                    <a href="#" class="btn btn-default btn-flat">Perfil</a>
                </div>

                <div class="pull-right">
                    @using (Html.BeginForm("Sair", "Conta", FormMethod.Post, new { id = "logoutForm" }))
                    {
                        @Html.AntiForgeryToken()
                        <a id="btn-logout" href="javascript:void(0)" class="btn btn-default btn-flat">Sair</a>
                    }
                </div>

            </li>
        </ul>
    </li>
}
else
{
    <ul class="nav navbar-nav navbar-right">
        <li><a href="/Account/Register">Register</a></li>
        <li><a href="/Account/Login">Login</a></li>
    </ul>
}